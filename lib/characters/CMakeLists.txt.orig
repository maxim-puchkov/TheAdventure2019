add_library(${libcharacters}
<<<<<<< HEAD
    src/Character.cpp
    src/CharacterManager.cpp
    src/NPC.cpp
    src/Avatar.cpp
    src/Attributes.cpp
    src/Inventory.cpp
=======
     src/Character.cpp
    #src/Avatar.cpp
    #src/PlayerControlledCharacter.cpp
    #src/NonPlayerControlledCharacter.cpp
    src/Attributes.cpp
    src/CharacterManager.cpp
>>>>>>> master_ref
)

target_include_directories(${libcharacters}
  PUBLIC
    $<INSTALL_INTERFACE:include>
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
  PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/src
)

target_link_libraries(${libcharacters}
    ${libworld}
    ${libobjects}
    ${libcharacterdb}
    ${libminigame}
    ${libcombat}
)

set_target_properties(${libcharacters}
                      PROPERTIES
                      LINKER_LANGUAGE CXX
                      CXX_STANDARD 17
)

install(TARGETS ${libcharacters}
  ARCHIVE DESTINATION lib
)
